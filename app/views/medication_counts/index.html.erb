<div id="page-wrapper">
  <div class="row">
    <p>&nbsp;</p>
    <div class="panel panel-default">
      <div class="panel-heading indiviuals_list">
        Medications List

      </div>
      <!-- /.panel-heading -->
      <div class="panel-body">
        <div class="table-responsive">
          <table class="table table-striped table-bordered table-hover">
            <thead>
            <tr>
              <th>S. No.</th>
              <th>Individual Name</th>
              <th>Date of Training</th>
              <th>Last Edited</th>
              <th colspan="3"></th>
            </tr>
            </thead>
            <tbody>
            <% @medication_counts.each_with_index do |medication_count, i| %>
                <tr>
                  <td><%= i + 1 %></td>
                  <td><%= medication_count.user.full_name if medication_count.user.present? %></td>
                  <td><%= medication_count.date %></td>
                  <td><%= medication_count.updated_at %></td>
                  <td><%= link_to edit_medication_count_path(medication_count) do %>
                        <%= image_tag 'setting.png' %> Edit
                    <% end %>
                  </td>
                  <td><%= link_to medication_count_path(medication_count) do %>
                        <i class="fa fa-eye"></i> View
                    <% end %>
                  </td>
                  <td><%= link_to  medication_count_path(medication_count,authenticity_token: form_authenticity_token), method: :delete, data: { confirm: 'Are you sure?' } do %>
                        <%= image_tag 'delete.png' %> Delete
                    <% end %>
                  </td>
                </tr>
                <tr>
            <% end %>
            </tbody>
          </table>
        </div>
        <!-- /.table-responsive -->
      </div>


      <!-- /.panel-body -->
    </div>
    <%= link_to 'Add New Entry', new_medication_count_path, class: 'btn add_entry' %>
  </div>
  <!-- /.row -->
</div>